.env
node_modules/
.wrangler/
*.log
.DS_Store

# 云厂商下载的密钥文件，绝不入库
*.csv
*.key
*.pem

# 第三方 skill 由 `npx skills add` 装入，属于本机工具链而非本项目源码。
# 这是个公开仓库，别把别人的合集连同 symlink 一起提交进来。
.agents/
.claude/skills/
skills-lock.json
